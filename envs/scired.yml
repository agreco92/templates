# neede for for code/02_analysis/04_sciRED_factor_analysis.py. pins an older numpy, so its separate from reticulate.yml 
#
# micromamba env create -f envs/scired.yml

name: templates2-scired
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
      - sciRED==1.2.1
      - scanpy==1.10.1
      - anndata==0.10.9
      - numpy==1.23.5
      - pandas==2.2.1
      - matplotlib==3.7.3
      - scikit-learn==1.4.2
      - statsmodels==0.14.1
      - PyYAML
